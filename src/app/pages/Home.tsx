import { Button } from "@src/app/components/ui/button";

const Home = () => (
  <section className="py-[6%] px-[20%] mt-[5%]">
    <h1 className="text-3xl font-bold">Hello world!</h1>
    <Button variant={"default"}>New</Button>
  </section>
);

export default Home;
